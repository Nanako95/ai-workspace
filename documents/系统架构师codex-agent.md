请帮我在我当前的工作目录（或桌面）下，创建一个名为 "codex-desktop-agent" 的完整自动化项目。
请严格执行以下步骤，并在每一步后向我确认进度：
### 1. 创建项目结构
- 在当前codex目录创建文件夹 `codex-desktop-agent`。
- 进入该文件夹。
### 2. 生成核心代码文件
请创建以下 3 个文件，内容必须完整且可运行：
**文件 A: `agent_server.py` (本地手脚)**
- 功能：启动一个 Flask HTTP 服务器，监听 5000 端口。
- 必须包含以下 API：
  - `GET /screenshot`: 使用 `pyautogui` 截图，返回 Base64 图片数据。
  - `POST /action`: 接收 JSON 指令 `{"cmd": "open|click|type|key", ...}`。
  - 内部调用 `pynput` 和 `pyautogui` 执行鼠标点击、键盘输入、打开应用（需支持 Windows 路径和 Mac 应用名）。
- 启动时打印提示：“✅ Codex 本地代理已启动！”。
**文件 B: `run_agent.py` (安全交互控制器)**
- 功能：作为主程序，启动安全的人机交互循环。
- 逻辑：
  1. 检查 `agent_server.py` 是否在运行，如果没有则自动尝试启动它（后台运行）。
  2. 进入 `while True` 循环，接收用户自然语言指令。
  3. **关键安全机制**：在执行任何动作前，必须打印动作详情（如“点击坐标 x,y”），并强制要求用户输入 `y` (确认) 或 `n` (跳过) 或 `s` (停止)。只有用户确认后才能真正调用 API。
  4. 包含一个模拟的 `get_llm_response` 函数（先做简单的规则匹配，比如包含“打开”就返回打开浏览器的动作），方便我测试。请在代码中注释说明这里将来要替换为真实的 LLM API 调用。
  5. 处理异常情况，确保不崩溃。
**文件 C: `requirements.txt`**
- 包含：`flask`, `pyautogui`, `pynput`, `requests`, `pillow`
### 3. 安装依赖
- 自动运行 `pip install -r requirements.txt` 安装所有库。
### 4. 启动引导
- 在所有文件创建完成后，请输出一段简短的 **“使用说明”**：
  - 告诉我如何启动程序（例如：`python run_agent.py`）。
  - 告诉我如何测试（例如：输入“打开浏览器”）。
  - 提醒我安全确认机制的存在。
**请现在开始执行！先创建文件夹和文件，打印完成状态，然后安装依赖。**