# AI Workspace 使用与更新指南

这份指南说明如何直接使用平台、在新电脑本地运行、更新版本并上传 GitHub，以及各目录的用途。

## 1. 直接使用 DataFlow

打开以下网址即可使用，无需安装 AI、Git、Node.js 或其他软件：

https://nanako95.github.io/ai-workspace/

基本步骤：

1. 打开网站。
2. 选择本地 CSV 文件，或加载示例数据。
3. 新增、删除或调整清洗规则。
4. 拖拽规则修改执行优先级。
5. 点击执行清洗，检查结果后导出 CSV。

CSV 在当前浏览器中处理，不会自动上传到 GitHub。不要把密码、客户数据或公司敏感资料提交到代码仓库。

## 1.1 直接使用小账本

打开以下 HTTPS 地址即可在手机或电脑使用：

https://little-ledger.onrender.com/

小账本支持用户名密码登录、手动记账、网页拍照/截图识别、多语言账目逐行识别、货币转换和月度/年度看板。账户与结构化账目保存在 Supabase，原始拍照图片只在浏览器内存中用于 OCR，不上传到云端或手机相册。

首次使用时，输入用户名和包含英文与数字的至少 6 位密码会自动注册；再次访问时使用相同用户名和密码登录。

## 2. 分享给其他人使用

把下面的网址通过微信、飞书或邮件发送给对方即可：

https://nanako95.github.io/ai-workspace/

对方只需要浏览器，不需要 GitHub 账号，也不需要安装 AI。

小账本链接也可以直接分享：

https://little-ledger.onrender.com/

手机可以将网站添加到主屏幕：

- iPhone Safari：点击分享按钮，再选择“添加到主屏幕”。
- Android Chrome：打开浏览器菜单，再选择“安装应用”或“添加到主屏幕”。
- 电脑 Chrome/Edge：点击地址栏右侧的安装图标（显示时）。

## 3. 在新电脑下载并本地运行

### 准备软件

需要安装：

- Git：https://git-scm.com/downloads
- Node.js LTS：https://nodejs.org/

只有需要修改或本地开发时才需要安装 AI/Codex。

### 第一次下载

打开 PowerShell，执行：

```powershell
git clone https://github.com/Nanako95/ai-workspace.git
cd ai-workspace\platform\dataflow-studio
npm install
npm run dev -- --port 4317
```

浏览器打开：

```text
http://127.0.0.1:4317/
```

### 以后再次启动

不需要重新下载或重新安装，执行：

```powershell
cd ai-workspace\platform\dataflow-studio
npm run dev -- --port 4317
```

也可以双击 `platform\dataflow-studio\start-dataflow.cmd`。

## 4. 获取 GitHub 上的最新版本

进入总仓库目录后执行：

```powershell
cd ai-workspace
git pull
```

如果 `package.json` 或 `package-lock.json` 有更新，再执行：

```powershell
cd platform\dataflow-studio
npm install
```

## 5. 修改后上传新版本

正式项目位于：

```text
ai-workspace\platform\dataflow-studio
```

修改并验证后，在 `ai-workspace` 根目录执行：

```powershell
git status
git add .
git commit -m "说明本次修改内容"
git push
```

命令作用：

- `git status`：查看本次发生变化的文件。
- `git add .`：把当前修改加入本次提交。
- `git commit`：在本地保存一个可以恢复的版本记录。
- `git push`：把新版本上传到 GitHub。

推送到 `main` 分支后，GitHub Actions 会自动构建并更新网站。通常等待一到几分钟，然后刷新网站；如果仍显示旧版，可按 `Ctrl + F5` 强制刷新。

可以在这里查看发布状态：

https://github.com/Nanako95/ai-workspace/actions

### 使用自动发布 skill

仓库内的 `skills\github-auto-update` 是用于本地完成修改后自动同步 GitHub 的技能。它会在当前分支上执行以下流程：

1. 检查远端是否领先、工作区是否有无关改动。
2. 检查常见密钥、凭据、缓存、`node_modules` 和构建产物，发现后停止发布。
3. 更新根目录 `CHANGELOG.md`，记录日期、目标、修改路径和验证情况。
4. 只暂存目标路径和 `CHANGELOG.md`，提交并推送到当前分支。
5. 用远端 commit 验证推送确实生效。

在新电脑上安装仓库中的 skill：

```powershell
git clone https://github.com/Nanako95/ai-workspace.git
Copy-Item -Recurse .\ai-workspace\skills\github-auto-update "$env:USERPROFILE\.codex\skills\github-auto-update"
```

之后让 AI 先阅读仓库 `AGENTS.md`、项目说明和这个 skill，再提出修改即可。默认发布当前仓库当前分支；远端冲突不会自动强推，必须先人工处理。

## 6. 在多台设备上修改时

每次开始修改前先同步：

```powershell
git pull
```

修改完成后再执行：

```powershell
git add .
git commit -m "说明本次修改内容"
git push
```

这样可以减少不同电脑同时修改造成的冲突。不要直接覆盖另一台电脑尚未推送的修改。

## 7. 让新设备上的 AI 接手

让 AI 先阅读项目规则：

```text
先阅读根目录 AGENTS.md，再阅读 platform/dataflow-studio/AGENTS.md 和 README.md。
我要修改 platform/dataflow-studio。完成后运行生产构建，检查修改内容，再提交并推送。
```

AI 修改完成后至少应运行：

```powershell
cd platform\dataflow-studio
npm install
npm run build -- --base=/ai-workspace/
```

## 8. 全仓库目录说明

```text
ai-workspace/
├─ platform/       可直接运行和发布的平台、网站、应用
├─ skills/         可复用的 AI/Codex 技能
├─ automations/    自动化脚本、定时任务和工作流
├─ documents/      非敏感模板与使用指南
├─ experiments/    尚未正式发布的实验项目
├─ archive/        已停止维护但需要保留的项目
├─ AGENTS.md       AI 修改整个仓库时必须遵守的规则
└─ README.md       仓库总览
```

当前可用平台：

- `platform/dataflow-studio`：CSV 数据质量检查、规则清洗、预览和导出。

以后新增项目时，根据类型放入对应目录，每个项目使用独立子文件夹。

## 9. 常见问题

### `git push` 提示未登录

执行：

```powershell
gh auth login --hostname github.com --git-protocol https --web
```

按照浏览器提示完成 GitHub 授权。

### 端口 4317 已被占用

换一个端口：

```powershell
npm run dev -- --port 4318
```

然后打开 `http://127.0.0.1:4318/`。

### 推送后网站没有更新

1. 打开 GitHub Actions 页面检查任务是否为绿色。
2. 等待一到几分钟。
3. 按 `Ctrl + F5` 强制刷新。
4. 如果任务为红色，点进失败任务查看日志并交给 AI 修复。

### 哪些文件不能上传

不要提交真实 CSV、Excel、密码、Token、API Key、客户名单或公司敏感资料。仓库已经忽略常见数据和环境变量文件，但上传前仍应先查看 `git status`。
